---
"@cloudflare/think": minor
---

Add the published `@cloudflare/think/voice` adapter with `createVoiceThink()` and `voiceChannel()`. The optional `@cloudflare/voice` peer powers Voice ingress, TTS, interruption handling, and channel-routed notices while Think Session remains the durable transcript.
