---
"@cloudflare/voice": minor
---

Add `streamRpcVoiceTurn()` and `VoiceRpcCallback` for streaming text from any RPC-connected Agent into the Voice pipeline, including remote cancellation on interruption.
